name: Harden-Runner - Fail
on:
  workflow_dispatch:
jobs:
  build:
    name: Deploy
    runs-on: ubuntu-slim
    steps:
      - name: Code Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 2
